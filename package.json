{
  "name": "survey-question-search",
  "version": "1.0.0",
  "description": "SurveyToGo question search deployed on Netlify (static React app + serverless API functions).",
  "main": "search.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix web",
    "build": "npm run build --prefix web",
    "dev": "netlify dev",
    "cli": "npm run cli --prefix server"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.18.1",
    "fast-xml-parser": "^5.10.1"
  }
}
